export enum ModalButtons {
    OK = "ok",
    YES = "yes",
    NO = "no",
    EXEC = "execute",
    CANCEL = "cancel",
    CLOSE = "close"
}

export enum ModalSizes {
    NORMAL = 0,
    SMALL = 1,
    LARGE = 2
}

export function ModalOptions(buttons: ModalButtons, opt_sizes: ModalSizes, opt_height: number): void {
    
}

export class Popup {
    
}